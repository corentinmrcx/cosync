<nav class="breadcrumb">
    {% for item in items %}
        {% if not loop.last %}
            <a href="{{ path(item.route, item.params ?? {}) }}" class="breadcrumb-link">{{ item.label }}</a>
            <span class="breadcrumb-sep">›</span>
        {% else %}
            <span class="breadcrumb-current">{{ item.label }}</span>
        {% endif %}
    {% endfor %}
</nav>
