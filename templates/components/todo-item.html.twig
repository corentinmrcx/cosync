{% import 'components/_icon.html.twig' as ui %}
<a href="{{ path(route, params ?? {}) }}" class="todo-item">
    <span class="todo-count">{{ count }}</span>
    <span class="todo-label">{{ label }}</span>
    <span class="todo-arrow">{{ ui.icon('arrow-right') }}</span>
</a>
