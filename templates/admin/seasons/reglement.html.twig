{% extends 'admin/layout.html.twig' %}

{% block title %}CoSync — Règlement {{ season.label }}{% endblock %}

{% block head %}
    <link href="https://cdn.quilljs.com/1.3.7/quill.snow.css" rel="stylesheet">
{% endblock %}

{% block content %}
{% include 'components/breadcrumb.html.twig' with { items: [
    { label: 'Tableau de bord', route: 'admin_dashboard' },
    { label: 'Configuration', route: 'admin_config_index' },
    { label: 'Règlement ' ~ season.label }
] } %}

<div class="reglement-edit-page">

    <form method="post" action="{{ path('admin_seasons_reglement', {id: season.id}) }}" class="reglement-edit-card" id="reglement-form">

        <input type="hidden" name="_token" value="{{ csrf_token('reglement_' ~ season.id) }}">
        <input type="hidden" name="reglement_text" id="reglement_text_input">

        <div class="reglement-edit-header">
            <h1 class="reglement-edit-title">Règlement intérieur — {{ season.label }}</h1>
            <p class="reglement-edit-hint">Ce texte sera affiché au licencié lors de l'étape de signature, et intégré dans le PDF archivé.</p>
        </div>

        <hr class="reglement-edit-divider">

        <div class="reglement-edit-field">
            <label class="reglement-edit-label">Texte du règlement</label>
            <div class="reglement-edit-editor-wrapper">
                <div id="quill-editor" class="reglement-edit-editor">{{ season.reglementText|raw }}</div>
            </div>
        </div>

        <div class="reglement-edit-actions">
            <a href="{{ path('admin_config_index') }}" class="reglement-edit-cancel">Annuler</a>
            <button type="submit" class="reglement-edit-submit">Enregistrer</button>
        </div>

    </form>

</div>

<script src="https://cdn.quilljs.com/1.3.7/quill.min.js"></script>
<script>
    const quill = new Quill('#quill-editor', {
        theme: 'snow',
        placeholder: 'Saisissez ici le règlement intérieur…',
        modules: {
            toolbar: [
                [{ header: [1, 2, 3, false] }],
                ['bold', 'italic', 'underline'],
                [{ list: 'ordered' }, { list: 'bullet' }],
                ['clean']
            ]
        }
    });

    document.getElementById('reglement-form').addEventListener('submit', function () {
        document.getElementById('reglement_text_input').value = quill.root.innerHTML;
    });
</script>
{% endblock %}
